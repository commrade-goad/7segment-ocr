# 7Segments LCD OCR

a simple RCNN model that can read 7segments display with: 0..9, '.', and '-'.

## Usage

1. Install all required package
```sh
pip install -r requirements.txt
```

2. Run the streamlit
```sh
streamlit run main.py
```
